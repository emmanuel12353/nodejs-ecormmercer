<nav class="navbar fixed-top navbar-dark navbar-expand-md bg-success text-white">
    <a class="text-white navbar-brand" href="/">
    <i class="fa fa-pagelines"></i> groccries stores
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
    aria-expanded="false" aria-label="Toggle navigation" >
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto menu"></ul>
    </div>
</nav>
<div class="container mt-4">
    <main role="main">
        <div class="row">
            <div class="col">
                <h1>Order</h1>
                <div class="table-responsive-sm">
               <table class="table table-striped">
               <thead>
                <tr>
                    <th>ID</th>
                    <th>Order</th>
                    <th>total</th>
                
                </tr>
               </thead>
               <tbody>
                {{#each order}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.order}}</td>
                    <td>{{this.total}}</td>
                    <td>
                        <a class="btn btn-primary" href="/order/{{this_id}}">
                        updates
                        </a>
                        <a class="btn btn-danger" href="/order/delete/{{this_id}}">
                        Delete
                        </a>
                    </td>
                </tr>

                {{/each}}
               </tbody>
               </table>
                </div>
            </div>
        </div>
    </main>
</div>
